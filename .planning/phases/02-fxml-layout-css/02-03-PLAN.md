---
phase: 02-fxml-layout-css
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/main/java/cs120/lab06/PrimaryController.java
  - src/main/java/cs120/lab06/App.java
autonomous: false
requirements_addressed:
  - GUI-02
  - GUI-03

must_haves:
  truths:
    - "mvn javafx:run displays game screen with dark #1a1a2e background"
    - "mvn javafx:run shows all five regions: found-words panel, letter area, guess display, score/info row, control buttons"
    - "foundWordsList ListView is bound to an ObservableList<String>"
    - "No setStyle() call exists in PrimaryController.java or App.java"
    - "All four control buttons are visible (Twist, Enter, Last Word, Clear)"
    - "Last Word button is visually disabled at startup"
  artifacts:
    - path: "src/main/java/cs120/lab06/PrimaryController.java"
      provides: "@FXML field declarations + initialize() wiring"
      contains: "@FXML private ListView<String> foundWordsList"
    - path: "src/main/java/cs120/lab06/App.java"
      provides: "CSS stylesheet attached to Scene; scene size 700x520"
      contains: "getStylesheets().add("
  key_links:
    - from: "App.java Scene"
      to: "styles.css"
      via: "scene.getStylesheets().add(App.class.getResource(\"styles.css\").toExternalForm())"
      pattern: "getStylesheets\\(\\)\\.add\\("
    - from: "PrimaryController.foundWordsList"
      to: "FXCollections.observableArrayList()"
      via: "foundWordsList.setItems(foundWords) in initialize()"
      pattern: "foundWordsList\\.setItems"
---

## Phase Goal

**As a** CS120 student, **I want to** run `mvn javafx:run` and see the fully styled game screen, **so that** I can confirm all layout regions render correctly with the dark theme before adding game logic in Phase 3.

<objective>
Wire the FXML layout (Plan 02) and the CSS file (Plan 01) to the running application.
Two files change: PrimaryController.java gains @FXML field declarations and an initialize()
that binds the ObservableList to the ListView; App.java attaches styles.css to the Scene
and updates scene dimensions to 700x520.

Purpose: This is the integration plan. Plans 01 and 02 create the static artifacts; Plan 03
makes them work together at runtime. After this plan, `mvn javafx:run` produces the styled
game screen from the Phase 2 success criteria.

Output: A running game screen with all five visible regions, dark theme applied, ListView
bound and ready for Phase 3 population.
</objective>

<execution_context>
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/phases/02-fxml-layout-css/02-CONTEXT.md
</execution_context>

<context>
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/ROADMAP.md
@/Users/andrewmcinnis/Desktop/TextTwist/CLAUDE.md

This plan depends on:
- 02-01-PLAN.md: styles.css at src/main/resources/cs120/lab06/styles.css
- 02-02-PLAN.md: primary.fxml with all fx:ids locked

@/Users/andrewmcinnis/Desktop/TextTwist/.planning/phases/02-fxml-layout-css/02-01-SUMMARY.md
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/phases/02-fxml-layout-css/02-02-SUMMARY.md

<interfaces>
From primary.fxml (created in Plan 02) — fx:ids that PrimaryController must match exactly:

  ListView:   fx:id="foundWordsList"
  FlowPane:   fx:id="letterButtonArea"
  HBox:       fx:id="guessDisplay"
  Label:      fx:id="scoreLabel"
  Label:      fx:id="levelLabel"
  Label:      fx:id="timeLabel"
  Button:     fx:id="twistBtn"
  Button:     fx:id="enterBtn"
  Button:     fx:id="lastWordBtn"
  Button:     fx:id="clearBtn"

From App.java (current Phase 1 stub — key signatures):

  public class App extends Application {
    private static Scene scene;

    public void start(Stage stage) throws IOException {
      scene = new Scene(loadFXML("primary"), 640, 480);  // <- resize to 700x520
      stage.setTitle("TextTwist");
      stage.setScene(scene);
      stage.show();
      // <- attach stylesheet here, after scene is created
    }

    private static Parent loadFXML(String fxml) throws IOException {
      FXMLLoader fxmlLoader = new FXMLLoader(App.class.getResource(fxml + ".fxml"));
      return fxmlLoader.load();
    }
  }

CSS resource URL pattern (App.class.getResource resolves within same classpath package):
  App.class.getResource("styles.css").toExternalForm()
  (App.class is in cs120.lab06; styles.css is also in cs120/lab06 resources — same package)
</interfaces>

Critical rules (CLAUDE.md):
- TwistController must have zero JavaFX imports — do NOT import TwistController in this plan
- No setStyle() calls anywhere
- No module-info.java — unnamed module path
- All styling via external CSS
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add @FXML declarations and ObservableList binding to PrimaryController</name>
  <files>src/main/java/cs120/lab06/PrimaryController.java</files>
  <read_first>
    - src/main/java/cs120/lab06/PrimaryController.java (MUST read current stub — understand existing initialize() and imports before editing)
    - src/main/resources/cs120/lab06/primary.fxml (confirm all fx:ids to match exactly — read completed Plan 02 output)
    - CLAUDE.md (TwistController zero-JavaFX-import rule; PrimaryController CAN have JavaFX imports)
    - .planning/research/ARCHITECTURE.md (§MVC Wiring — PrimaryController field declarations and initialize() pattern)
  </read_first>
  <action>
Replace src/main/java/cs120/lab06/PrimaryController.java with a new version that adds
@FXML field declarations and a proper initialize() method. Keep the class in package cs120.lab06.

Imports required:
  javafx.fxml.FXML
  javafx.scene.control.ListView
  javafx.scene.control.Label
  javafx.scene.control.Button
  javafx.scene.layout.FlowPane
  javafx.scene.layout.HBox
  javafx.collections.FXCollections
  javafx.collections.ObservableList

Field declarations (all @FXML annotated, all private):
  @FXML private ListView<String> foundWordsList;
  @FXML private FlowPane letterButtonArea;
  @FXML private HBox guessDisplay;
  @FXML private Label scoreLabel;
  @FXML private Label levelLabel;
  @FXML private Label timeLabel;
  @FXML private Button twistBtn;
  @FXML private Button enterBtn;
  @FXML private Button lastWordBtn;
  @FXML private Button clearBtn;

Non-FXML fields (needed now for Phase 3 wiring, declared but not initialized yet):
  private ObservableList<String> foundWords;

initialize() method (annotated @FXML, public void, no parameters):
  foundWords = FXCollections.observableArrayList();
  foundWordsList.setItems(foundWords);
  (Nothing else — no game model creation, no timer, no startNewEpisode. Those come in Phase 3.)

Do NOT add any event handler methods. Do NOT import or instantiate TwistController or
WordDictionary (that is Phase 3 work). Do NOT call setStyle() on any node.
Do NOT add any statements that read or modify the timeLabel, scoreLabel, or levelLabel
text — the FXML sets initial text values ("Score: 0", "Level: 1", "2:00").
  </action>
  <verify>
    <automated>grep -c "@FXML private ListView" /Users/andrewmcinnis/Desktop/TextTwist/src/main/java/cs120/lab06/PrimaryController.java && grep -c "foundWordsList.setItems" /Users/andrewmcinnis/Desktop/TextTwist/src/main/java/cs120/lab06/PrimaryController.java && grep -c "setStyle" /Users/andrewmcinnis/Desktop/TextTwist/src/main/java/cs120/lab06/PrimaryController.java</automated>
  </verify>
  <done>
    - PrimaryController.java compiles (mvn compile succeeds)
    - All 10 @FXML fields declared with matching fx:id names from primary.fxml
    - foundWordsList.setItems(foundWords) called in initialize()
    - No TwistController or WordDictionary imports
    - No setStyle() calls
    - No event handler methods
    - grep -c "setStyle" returns 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Attach CSS stylesheet and resize Scene in App.java, then verify runtime render</name>
  <files>src/main/java/cs120/lab06/App.java</files>
  <read_first>
    - src/main/java/cs120/lab06/App.java (MUST read current file — understand current scene creation and loadFXML pattern before editing)
    - src/main/resources/cs120/lab06/styles.css (confirm it exists at this path — created in Plan 01)
    - .planning/phases/02-fxml-layout-css/02-CONTEXT.md (Claude's Discretion: CSS via App.java getStylesheets().add() is the chosen approach; scene size ~700x520)
  </read_first>
  <action>
Edit src/main/java/cs120/lab06/App.java. Make exactly two changes to the start() method:

Change 1 — Scene dimensions: change 640, 480 to 700, 520 in the Scene constructor call.

Change 2 — Attach stylesheet: immediately after the scene = new Scene(...) line, add:
  scene.getStylesheets().add(App.class.getResource("styles.css").toExternalForm());

The App.class.getResource("styles.css") call works because App.class is in package cs120.lab06
and styles.css is at src/main/resources/cs120/lab06/styles.css — same package, same classpath
location. No path prefix is needed.

Do NOT add a null check for getResource() — if styles.css is missing, a NullPointerException
here is the correct early failure (makes the missing file obvious rather than silently running
with no styles).

Do NOT change any other part of App.java (loadFXML, setRoot, main).
Do NOT call setStyle() anywhere.
Do NOT add any import for TwistController or WordDictionary.

After editing, run: mvn javafx:run
Verify the running window shows:
- Dark background (#1a1a2e — near-black navy, not default gray)
- Left panel with "FOUND WORDS" header and empty ListView
- Center area with "Score: 0", "Level: 1", "2:00" labels
- Empty FlowPane area with visible border (letter button region)
- Empty HBox with visible border (guess display region)
- Bottom row with Twist, Enter, Last Word (disabled), Clear buttons
- Window is approximately 700x520px

This is the human-verify checkpoint — the executor runs the app and visually confirms
all regions are visible before marking the plan complete.
  </action>
  <verify>
    <automated>grep "getStylesheets().add(" /Users/andrewmcinnis/Desktop/TextTwist/src/main/java/cs120/lab06/App.java && grep "700, 520" /Users/andrewmcinnis/Desktop/TextTwist/src/main/java/cs120/lab06/App.java && grep -r "setStyle" /Users/andrewmcinnis/Desktop/TextTwist/src/main/java/ | grep -v "^Binary" | wc -l</automated>
  </verify>
  <done>
    - App.java contains scene.getStylesheets().add(App.class.getResource("styles.css").toExternalForm())
    - Scene dimensions are 700, 520
    - mvn compile succeeds with no errors
    - No setStyle() in any .java file under src/main/java/ (grep returns 0)
  </done>
</task>

</tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    App.java now attaches styles.css to the Scene (700x520). PrimaryController declares all
    @FXML fields and binds foundWordsList to an ObservableList. Running the app should display
    the full styled game screen for the first time.
  </what-built>
  <how-to-verify>
    1. From the project root: mvn javafx:run
    2. Window should open at approximately 700x520px titled "TextTwist"
    3. Verify background is dark navy (not white/gray) — approximately #1a1a2e
    4. Left column: "FOUND WORDS" label above an empty scrollable list area with dark background
    5. Upper center: "Score: 0", "Level: 1", "2:00" labels on one line
    6. Center: empty bordered region (letter button area — no buttons yet, that's Phase 3)
    7. Below: empty bordered horizontal strip (guess display area — empty, that's Phase 3)
    8. Bottom: four buttons in a row — "Twist", "Enter", "Last Word" (appears grayed/disabled), "Clear"
    9. All button text is readable (light text on dark buttons)
    10. No Java exception appears in the terminal where you ran mvn javafx:run
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 checks pass, or describe which regions are missing/wrong</resume-signal>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.class.getResource("styles.css") | Classpath resource lookup — null if styles.css is misplaced |
| @FXML injection | FXMLLoader injects by field name matching fx:id; mismatch → field stays null → NPE at first use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Denial of Service | App.getResource("styles.css") null | accept | NullPointerException on startup is the correct fast-fail; styles.css absence is caught immediately rather than silently running unstyled. Plan 01 creates the file; if it is missing, Plan 01 was skipped (wave dependency violation). |
| T-02-06 | Tampering | @FXML field name vs fx:id mismatch | mitigate | Acceptance criteria grep verifies foundWordsList.setItems() call exists; mvn compile catches type mismatches; visual verification checkpoint catches null-panel failures at runtime |
| T-02-07 | Information Disclosure | TwistController zero-JavaFX-import rule | mitigate | Task 1 action explicitly prohibits importing TwistController or WordDictionary; acceptance criteria: grep -r "import cs120.lab06.TwistController" src/main/java/cs120/lab06/PrimaryController.java returns nothing |
</threat_model>

<verification>
Run these after plan completes:

1. Compile: mvn compile (must succeed, zero errors)
2. CSS attachment: grep "getStylesheets().add(" src/main/java/cs120/lab06/App.java (must return 1 match)
3. Scene size: grep "700, 520" src/main/java/cs120/lab06/App.java (must return 1 match)
4. All @FXML fields: grep "@FXML" src/main/java/cs120/lab06/PrimaryController.java | wc -l (must return 10 or more — 10 node fields + initialize method annotation)
5. ObservableList binding: grep "foundWordsList.setItems" src/main/java/cs120/lab06/PrimaryController.java (must return 1 match)
6. No setStyle(): grep -r "setStyle" src/main/java/ (must return zero results)
7. No TwistController import in PrimaryController: grep "TwistController" src/main/java/cs120/lab06/PrimaryController.java (must return zero results — Phase 3 adds this)
8. Runtime: mvn javafx:run + human checkpoint above
</verification>

<success_criteria>
Phase 2 complete when:
1. mvn javafx:run opens a 700x520 window with dark background, all five regions visible, gold/dark color scheme
2. No setStyle() call exists anywhere in src/main/java/ (grep returns 0)
3. foundWordsList ListView is bound to ObservableList (foundWordsList.setItems(foundWords) in initialize())
4. mvn compile succeeds with zero errors or warnings
5. SceneBuilder can open primary.fxml without errors (all fx:ids resolve — confirmed by compile + @FXML matching)
6. Human checkpoint approved: all 10 visual checks pass
</success_criteria>

<output>
After completion, create .planning/phases/02-fxml-layout-css/02-03-SUMMARY.md
</output>
