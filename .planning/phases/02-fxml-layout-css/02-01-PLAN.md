---
phase: 02-fxml-layout-css
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/cs120/lab06/styles.css
autonomous: true
requirements_addressed:
  - GUI-02

must_haves:
  truths:
    - "styles.css exists at src/main/resources/cs120/lab06/styles.css"
    - "No setStyle() call exists in any Java file in src/main/java/"
    - "CSS defines .letter-btn, .timer-warning, .timer-critical, .letter-slot selectors"
    - "All color values are JavaFX -fx- prefixed properties, not bare CSS properties"
  artifacts:
    - path: "src/main/resources/cs120/lab06/styles.css"
      provides: "All visual rules for the game screen"
      contains: ".letter-btn"
    - path: "src/main/resources/cs120/lab06/styles.css"
      provides: "Timer state classes"
      contains: ".timer-warning"
    - path: "src/main/resources/cs120/lab06/styles.css"
      provides: "Guess display tile class"
      contains: ".letter-slot"
  key_links:
    - from: "styles.css"
      to: "PrimaryController.java (Phase 3)"
      via: "CSS class names .timer-warning and .timer-critical"
      pattern: "\\.timer-warning"
    - from: "styles.css"
      to: "primary.fxml"
      via: ".root background color on BorderPane"
      pattern: "\\.root"
---

## Phase Goal

**As a** CS120 student, **I want to** see the TextTwist game screen styled with a dark game aesthetic, **so that** the UI looks intentionally designed and earns the "impeccable style" rubric points.

<objective>
Create src/main/resources/cs120/lab06/styles.css from scratch, defining all visual rules for
the game screen. This is the sole source of styling — no inline setStyle() calls will exist
anywhere in the Java source. The CSS file must define every selector that Phase 3 will
reference dynamically, so Phase 3 can add/remove CSS classes without creating new ones.

Purpose: Establish the visual contract. Phase 3 and Phase 4 reference class names defined
here; they never call setStyle(). "Impeccable style" is a graded criterion.

Output: src/main/resources/cs120/lab06/styles.css — a single CSS file containing all rules.
</objective>

<execution_context>
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/phases/02-fxml-layout-css/02-CONTEXT.md
</execution_context>

<context>
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/ROADMAP.md
@/Users/andrewmcinnis/Desktop/TextTwist/CLAUDE.md

Key constraints from CLAUDE.md and CONTEXT.md:
- All styling via external CSS — no setStyle() calls anywhere in Java source
- CSS class names are LOCKED: .letter-btn, .timer-warning, .timer-critical, .letter-slot
- JavaFX CSS requires -fx- prefix on all properties (e.g., -fx-background-color, not background-color)
- Bare CSS property names without -fx- prefix are silently ignored by JavaFX

Color palette (locked by D-05 through D-10):
- Root background: #1a1a2e
- Letter buttons base: #f0a500 (gold), text: #1a1a2e (dark)
- Letter buttons disabled: opacity 0.4, background #555
- Found-words panel: #16213e, border: #0f3460
- Control buttons: #0f3460 background, #e0e0e0 text
- All text on dark: #e0e0e0
- Timer warning (<=20s): amber/yellow text
- Timer critical (<=10s): red text

Timer class accumulation pitfall (from PITFALLS.md §3, CLAUDE.md):
- Phase 3 removes old class before adding new — define .timer-warning and .timer-critical
  as standalone selectors (not cascading from one another)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create styles.css with all game screen visual rules</name>
  <files>src/main/resources/cs120/lab06/styles.css</files>
  <read_first>
    - src/main/resources/cs120/lab06/primary.fxml (current stub — understand what nodes exist today; Phase 2 plan 02 will replace it with BorderPane, but CSS .root applies to root node regardless)
    - CLAUDE.md (critical rules: no setStyle(), locked CSS class names)
    - .planning/phases/02-fxml-layout-css/02-CONTEXT.md (D-05 through D-13 for exact values)
  </read_first>
  <action>
Create src/main/resources/cs120/lab06/styles.css. This file does not exist yet — create it from scratch.

The file must define the following selectors in this logical order. Use only -fx- prefixed property names (JavaFX silently ignores bare CSS names):

SECTION 1 — Root and background:
.root selector: -fx-background-color: #1a1a2e; -fx-font-family: "System"; -fx-text-fill: #e0e0e0;

SECTION 2 — Found-words panel (left BorderPane region, applied to ListView):
.found-words-list selector: -fx-background-color: #16213e; -fx-border-color: #0f3460; -fx-border-insets: 2; -fx-border-width: 1; -fx-control-inner-background: #16213e; -fx-text-fill: #e0e0e0;
Also define .found-words-list .list-cell: -fx-background-color: transparent; -fx-text-fill: #e0e0e0; -fx-font-size: 13px;
And .found-words-list .list-cell:selected: -fx-background-color: #0f3460;

SECTION 3 — Score/info labels (applied to scoreLabel, levelLabel, timeLabel):
.info-label selector: -fx-text-fill: #e0e0e0; -fx-font-size: 14px; -fx-font-weight: bold;
.time-label selector: -fx-text-fill: #e0e0e0; -fx-font-family: monospace; -fx-font-size: 24px; -fx-font-weight: bold;

SECTION 4 — Timer state classes (Phase 3 adds/removes these on timeLabel):
.timer-warning selector (<=20s): -fx-text-fill: #f0a500; (amber — same gold as letter buttons for visual consistency)
.timer-critical selector (<=10s): -fx-text-fill: #e53935; -fx-font-weight: bold; (red, bold for urgency)
These must be STANDALONE selectors — not cascading from .time-label — so Phase 3 can add one and remove the other without accumulation side effects.

SECTION 5 — Letter button area (FlowPane container):
.letter-area selector: -fx-background-color: #16213e; -fx-border-color: #0f3460; -fx-border-width: 1; -fx-border-radius: 4; -fx-background-radius: 4; -fx-padding: 10 10 10 10; -fx-min-height: 60;

SECTION 6 — Letter buttons (applied dynamically in Phase 3 via getStyleClass().add("letter-btn")):
.letter-btn selector: -fx-background-color: #f0a500; -fx-text-fill: #1a1a2e; -fx-font-weight: bold; -fx-font-size: 15px; -fx-padding: 10 14 10 14; -fx-background-radius: 8; -fx-border-radius: 8; -fx-cursor: hand; -fx-min-width: 40;
.letter-btn:hover selector: -fx-background-color: #ffc040; (lighter gold on hover)
.letter-btn:disabled selector: -fx-opacity: 0.4; -fx-background-color: #555555; (grayed-out when used)

SECTION 7 — Guess display area (HBox container with fx:id=guessDisplay):
.guess-display selector: -fx-background-color: #16213e; -fx-border-color: #0f3460; -fx-border-width: 1; -fx-border-radius: 4; -fx-background-radius: 4; -fx-padding: 8 10 8 10; -fx-alignment: center; -fx-min-height: 50; -fx-min-width: 200;

SECTION 8 — Guess display letter tiles (Phase 3 adds Labels with this class):
.letter-slot selector: -fx-background-color: #f0a500; -fx-text-fill: #1a1a2e; -fx-font-family: monospace; -fx-font-size: 18px; -fx-font-weight: bold; -fx-padding: 8 10 8 10; -fx-background-radius: 4; -fx-border-radius: 4; -fx-min-width: 32; -fx-alignment: center;
(This class is defined here even though Phase 3 creates the labels dynamically.)

SECTION 9 — Visual feedback classes (Phase 3/4 toggles these on guessDisplay):
.guess-correct selector: -fx-border-color: #43a047; -fx-border-width: 2;
.guess-invalid selector: -fx-border-color: #e53935; -fx-border-width: 2;

SECTION 10 — Control buttons row (Twist, Enter, Last Word, Clear):
.control-btn selector: -fx-background-color: #0f3460; -fx-text-fill: #e0e0e0; -fx-font-size: 13px; -fx-font-weight: bold; -fx-padding: 8 16 8 16; -fx-background-radius: 6; -fx-border-radius: 6; -fx-cursor: hand;
.control-btn:hover selector: -fx-background-color: #1a4a8a;
.control-btn:disabled selector: -fx-opacity: 0.4;

SECTION 11 — Section labels and layout helpers:
.section-header selector: -fx-text-fill: #888888; -fx-font-size: 11px;
.center-vbox selector: -fx-spacing: 12; -fx-padding: 10 14 10 14;
.info-hbox selector: -fx-padding: 4 0 4 0; -fx-spacing: 20;
.control-hbox selector: -fx-spacing: 8; -fx-padding: 4 0 4 0;

Do NOT add any setStyle() call anywhere. Do NOT use bare CSS property names without -fx- prefix. Do NOT define any selector that conflicts with the locked names in CLAUDE.md.
  </action>
  <verify>
    <automated>grep -c "\.letter-btn" /Users/andrewmcinnis/Desktop/TextTwist/src/main/resources/cs120/lab06/styles.css && grep -c "\.timer-warning" /Users/andrewmcinnis/Desktop/TextTwist/src/main/resources/cs120/lab06/styles.css && grep -c "\.timer-critical" /Users/andrewmcinnis/Desktop/TextTwist/src/main/resources/cs120/lab06/styles.css && grep -c "\.letter-slot" /Users/andrewmcinnis/Desktop/TextTwist/src/main/resources/cs120/lab06/styles.css</automated>
  </verify>
  <done>
    - styles.css exists at src/main/resources/cs120/lab06/styles.css
    - File contains all four locked class names: .letter-btn, .timer-warning, .timer-critical, .letter-slot
    - All property values use -fx- prefix
    - No setStyle() string appears anywhere in the file
    - Root background is #1a1a2e, letter button base color is #f0a500, found-words panel is #16213e
    - .letter-btn:disabled reduces opacity to 0.4 with gray background #555555
    - .timer-warning and .timer-critical are standalone selectors (not nested under .time-label)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS class names → Phase 3 code | Phase 3 calls getStyleClass().add() with string literals; a typo in CSS or in Phase 3 code silently produces no visual change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | styles.css class names | mitigate | CSS class names .letter-btn, .timer-warning, .timer-critical, .letter-slot are locked in CLAUDE.md; acceptance criteria grep verifies presence; Phase 3 uses these exact names as string literals |
| T-02-02 | Information Disclosure | CSS bare property names | accept | JavaFX silently ignores non-prefixed properties; no security risk, purely functional; mitigated by using -fx- prefix on all properties |
</threat_model>

<verification>
Run these after plan completes:

1. File existence: ls -la src/main/resources/cs120/lab06/styles.css (must exist, non-zero size)
2. Locked class names present: grep -E "\.(letter-btn|timer-warning|timer-critical|letter-slot)" src/main/resources/cs120/lab06/styles.css (must return 4+ matches)
3. No setStyle() in Java: grep -r "setStyle" src/main/java/ (must return no results)
4. No bare CSS properties: grep -v "^\s*/\*" src/main/resources/cs120/lab06/styles.css | grep -v "-fx-" | grep ":" (should return only selector lines and comments, no bare property declarations)
</verification>

<success_criteria>
- src/main/resources/cs120/lab06/styles.css exists and is non-empty
- All four locked CSS class names are defined: .letter-btn, .letter-slot, .timer-warning, .timer-critical
- Root background rule uses -fx-background-color: #1a1a2e
- Letter button base uses -fx-background-color: #f0a500
- All property values use -fx- prefix
- No setStyle() exists in any .java file under src/main/java/
</success_criteria>

<output>
After completion, create .planning/phases/02-fxml-layout-css/02-01-SUMMARY.md
</output>
