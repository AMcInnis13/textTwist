---
phase: 02-fxml-layout-css
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/cs120/lab06/primary.fxml
autonomous: true
requirements_addressed:
  - GUI-01
  - GUI-03
  - GUI-04
  - GUI-05
  - GUI-06

must_haves:
  truths:
    - "FXML root is a BorderPane (not the Phase 1 VBox stub)"
    - "Left region contains a ListView with fx:id=foundWordsList and prefWidth=180"
    - "Center VBox has four children: info HBox, letterButtonArea FlowPane, guessDisplay HBox, control HBox"
    - "Score/level/time labels exist with fx:ids scoreLabel, levelLabel, timeLabel"
    - "All four control buttons exist with fx:ids twistBtn, enterBtn, lastWordBtn, clearBtn"
    - "SceneBuilder opens the file without errors"
  artifacts:
    - path: "src/main/resources/cs120/lab06/primary.fxml"
      provides: "Full game screen FXML layout"
      contains: "BorderPane"
    - path: "src/main/resources/cs120/lab06/primary.fxml"
      provides: "All fx:id references for PrimaryController"
      contains: "fx:id=\"foundWordsList\""
  key_links:
    - from: "primary.fxml fx:id=foundWordsList"
      to: "PrimaryController.foundWordsList field"
      via: "@FXML injection by FXMLLoader"
      pattern: "fx:id=\"foundWordsList\""
    - from: "primary.fxml fx:id=letterButtonArea"
      to: "PrimaryController.letterButtonArea field"
      via: "@FXML injection"
      pattern: "fx:id=\"letterButtonArea\""
    - from: "primary.fxml"
      to: "styles.css"
      via: "stylesheets attribute or App.java (wired in Plan 03)"
      pattern: "fx:controller=\"cs120.lab06.PrimaryController\""
---

## Phase Goal

**As a** CS120 student, **I want to** open primary.fxml in SceneBuilder and see a complete game screen layout with all five UI regions correctly placed, **so that** I can verify the structure before wiring event handlers in Phase 3.

<objective>
Completely replace src/main/resources/cs120/lab06/primary.fxml. The Phase 1 stub (VBox with a
placeholder Label) is discarded. The new file defines the full game screen layout using
BorderPane as the root, with all five required UI regions present and all fx:id attributes
that PrimaryController will inject in Plan 03.

Purpose: Establish the locked fx:id contract. Plan 03 (PrimaryController) depends on these
fx:ids being finalized. Phase 3 event handlers wire to the same nodes — changing any fx:id
after this plan breaks Phase 3.

Output: src/main/resources/cs120/lab06/primary.fxml — complete FXML layout, all fx:ids
present, opens without errors in SceneBuilder and at runtime.
</objective>

<execution_context>
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/phases/02-fxml-layout-css/02-CONTEXT.md
</execution_context>

<context>
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/ROADMAP.md
@/Users/andrewmcinnis/Desktop/TextTwist/CLAUDE.md

Locked layout decisions (from CONTEXT.md D-01 through D-13):

ROOT: BorderPane, fx:controller="cs120.lab06.PrimaryController"
FXML namespace: xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"

LEFT: ListView (type="java.lang.String"), fx:id=foundWordsList, prefWidth=180,
      styleClass=found-words-list. Region header label "FOUND WORDS" above or inside.

CENTER: VBox, styleClass=center-vbox, with these children in order:
  1. HBox (info panel), styleClass=info-hbox, spacing=20:
       - Label fx:id=scoreLabel, text="Score: 0", styleClass=info-label
       - Label fx:id=levelLabel, text="Level: 1", styleClass=info-label
       - Label fx:id=timeLabel, text="2:00", styleClass="info-label time-label"
  2. FlowPane fx:id=letterButtonArea, styleClass=letter-area, hgap=8, vgap=8, minHeight=70,
       alignment=CENTER, prefWrapLength=400
  3. HBox fx:id=guessDisplay, styleClass=guess-display, spacing=4, alignment=CENTER,
       minHeight=50, minWidth=200
  4. HBox (control buttons), styleClass=control-hbox, spacing=8, alignment=CENTER:
       - Button fx:id=twistBtn, text="Twist", styleClass=control-btn
       - Button fx:id=enterBtn, text="Enter", styleClass=control-btn
       - Button fx:id=lastWordBtn, text="Last Word", styleClass=control-btn, disable=true
       - Button fx:id=clearBtn, text="Clear", styleClass=control-btn

FXML imports needed:
  javafx.scene.layout.BorderPane, VBox, HBox, FlowPane
  javafx.scene.control.ListView, Label, Button
  javafx.geometry.Insets, Pos (for alignment constants)

CSS classes applied in FXML (defined in styles.css Plan 01):
  .found-words-list on ListView
  .center-vbox on center VBox
  .info-hbox on info HBox
  .info-label on score/level Labels
  .time-label added to timeLabel's styleClass list alongside info-label
  .letter-area on FlowPane
  .guess-display on guessDisplay HBox
  .control-hbox on control buttons HBox
  .control-btn on each control Button

Architecture note (ARCHITECTURE.md §Build Order):
  Letter buttons are NOT placed in FXML — Phase 3 creates them dynamically in
  startNewEpisode(). The FlowPane (letterButtonArea) is empty in this FXML.
  The guessDisplay HBox is also empty — Phase 3 adds Label children programmatically.
  Do NOT add placeholder Letter buttons or guess slot Labels here.

lastWordBtn starts with disable="true" in FXML — Phase 3 enables it after first
successful word guess per episode (PITFALLS.md §8, CONTEXT.md D-10).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace primary.fxml with full BorderPane game screen layout</name>
  <files>src/main/resources/cs120/lab06/primary.fxml</files>
  <read_first>
    - src/main/resources/cs120/lab06/primary.fxml (MUST read current stub before overwriting — confirm it is the Phase 1 VBox stub and not something newer)
    - CLAUDE.md (critical: fx:controller must be cs120.lab06.PrimaryController; no inline styling)
    - .planning/phases/02-fxml-layout-css/02-CONTEXT.md (D-01 through D-13 for exact fx:ids, layout structure, node types)
  </read_first>
  <action>
Overwrite src/main/resources/cs120/lab06/primary.fxml completely. Remove the Phase 1 VBox stub and the placeholder Label.

Write a complete FXML document with this structure:

XML declaration: <?xml version="1.0" encoding="UTF-8"?>

Processing instructions (<?import ...?>) for:
  javafx.scene.layout.BorderPane
  javafx.scene.layout.VBox
  javafx.scene.layout.HBox
  javafx.scene.layout.FlowPane
  javafx.scene.control.ListView
  javafx.scene.control.Label
  javafx.scene.control.Button
  javafx.geometry.Insets

Root element: BorderPane
  xmlns="http://javafx.com/javafx/21"
  xmlns:fx="http://javafx.com/fxml/1"
  fx:controller="cs120.lab06.PrimaryController"
  styleClass="root"

BorderPane left child: ListView
  fx:id="foundWordsList"
  prefWidth="180"
  styleClass="found-words-list"
  Wrap in a VBox with a "FOUND WORDS" header Label (styleClass="section-header",
  padding Insets top=8 left=8) so the panel has a visible title in Phase 2.
  The VBox itself: prefWidth=180, styleClass="found-words-panel" (planner discretion).
  Set BorderPane.margin on this VBox to Insets(8,0,8,8) for visual separation.

BorderPane center child: VBox
  styleClass="center-vbox"
  spacing="12"
  padding: Insets(10, 14, 10, 14)
  BorderPane.margin: Insets(8, 8, 8, 4)

  Child 1 of center VBox — HBox (score/info row):
    styleClass="info-hbox"
    spacing="20"
    alignment="CENTER_LEFT"
    Children:
      Label fx:id="scoreLabel" text="Score: 0" styleClass="info-label"
      Label fx:id="levelLabel" text="Level: 1" styleClass="info-label"
      Label fx:id="timeLabel"  text="2:00"     styleClass="info-label time-label"
        (two space-separated class names in styleClass attribute)

  Child 2 of center VBox — FlowPane (letter buttons area, empty in Phase 2):
    fx:id="letterButtonArea"
    styleClass="letter-area"
    hgap="8"
    vgap="8"
    prefWrapLength="400"
    alignment="CENTER"
    minHeight="70"

  Child 3 of center VBox — HBox (guess display area, empty in Phase 2):
    fx:id="guessDisplay"
    styleClass="guess-display"
    spacing="4"
    alignment="CENTER"
    minHeight="50"
    minWidth="200"

  Child 4 of center VBox — HBox (control buttons row):
    styleClass="control-hbox"
    spacing="8"
    alignment="CENTER"
    Children (in this exact order, left to right):
      Button fx:id="twistBtn"    text="Twist"     styleClass="control-btn"
      Button fx:id="enterBtn"    text="Enter"     styleClass="control-btn"
      Button fx:id="lastWordBtn" text="Last Word" styleClass="control-btn" disable="true"
      Button fx:id="clearBtn"    text="Clear"     styleClass="control-btn"

Do NOT add a stylesheet reference inside the FXML — App.java will attach it (Plan 03).
Do NOT place any Letter buttons or guess-slot Labels inside letterButtonArea or guessDisplay.
Do NOT add any onAction attributes to any buttons — Phase 3 wires those handlers.
Do NOT use inline style attributes on any element.
  </action>
  <verify>
    <automated>grep -c "fx:id=\"foundWordsList\"" /Users/andrewmcinnis/Desktop/TextTwist/src/main/resources/cs120/lab06/primary.fxml && grep -c "fx:id=\"letterButtonArea\"" /Users/andrewmcinnis/Desktop/TextTwist/src/main/resources/cs120/lab06/primary.fxml && grep -c "fx:id=\"guessDisplay\"" /Users/andrewmcinnis/Desktop/TextTwist/src/main/resources/cs120/lab06/primary.fxml && grep -c "fx:id=\"twistBtn\"" /Users/andrewmcinnis/Desktop/TextTwist/src/main/resources/cs120/lab06/primary.fxml && grep -c "fx:id=\"scoreLabel\"" /Users/andrewmcinnis/Desktop/TextTwist/src/main/resources/cs120/lab06/primary.fxml && grep -c "BorderPane" /Users/andrewmcinnis/Desktop/TextTwist/src/main/resources/cs120/lab06/primary.fxml</automated>
  </verify>
  <done>
    - primary.fxml root element is BorderPane (not VBox)
    - Phase 1 stub VBox and placeholder Label are gone
    - fx:id=foundWordsList on a ListView with prefWidth=180
    - fx:id=letterButtonArea on a FlowPane (empty, no children)
    - fx:id=guessDisplay on an HBox (empty, no children)
    - fx:id=scoreLabel, fx:id=levelLabel, fx:id=timeLabel all present
    - fx:id=twistBtn, fx:id=enterBtn, fx:id=lastWordBtn (disable=true), fx:id=clearBtn all present
    - No onAction attributes on any node
    - No inline style attributes on any node
    - No placeholder letter buttons or guess-slot labels
    - fx:controller="cs120.lab06.PrimaryController" on root element
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FXML fx:ids → Java @FXML fields | Mismatch causes silent null injection — node is null at runtime, NullPointerException deferred until first use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | FXML fx:id contract | mitigate | All fx:ids are locked per CONTEXT.md D-03; acceptance criteria grep verifies each required fx:id is present; Plan 03 declares matching @FXML field names |
| T-02-04 | Denial of Service | lastWordBtn disable=true | mitigate | lastWordBtn starts disabled in FXML per D-10 and PITFALLS.md §8, preventing NPE on first click before any word is found |
</threat_model>

<verification>
Run these after plan completes:

1. Root node check: grep "BorderPane" src/main/resources/cs120/lab06/primary.fxml (must appear at least twice — import and element)
2. All fx:ids: grep -E "fx:id=\"(foundWordsList|letterButtonArea|guessDisplay|scoreLabel|levelLabel|timeLabel|twistBtn|enterBtn|lastWordBtn|clearBtn)\"" src/main/resources/cs120/lab06/primary.fxml (must return 10 lines)
3. No onAction: grep "onAction" src/main/resources/cs120/lab06/primary.fxml (must return nothing)
4. No inline style: grep "style=\"" src/main/resources/cs120/lab06/primary.fxml (must return nothing)
5. No placeholder children in dynamic areas: grep -A5 "letterButtonArea" src/main/resources/cs120/lab06/primary.fxml (FlowPane should be self-closing or have empty children list)
6. lastWordBtn disabled: grep "lastWordBtn" src/main/resources/cs120/lab06/primary.fxml | grep "disable"
</verification>

<success_criteria>
- primary.fxml root is BorderPane, not VBox
- All 10 fx:id attributes present and correctly named (foundWordsList, letterButtonArea, guessDisplay, scoreLabel, levelLabel, timeLabel, twistBtn, enterBtn, lastWordBtn, clearBtn)
- letterButtonArea FlowPane and guessDisplay HBox have no children in FXML
- lastWordBtn has disable="true"
- No onAction attributes on any button
- No inline style="" attributes on any element
- File parses as valid XML (mvn compile succeeds)
</success_criteria>

<output>
After completion, create .planning/phases/02-fxml-layout-css/02-02-SUMMARY.md
</output>
