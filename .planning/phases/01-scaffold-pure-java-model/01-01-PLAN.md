---
id: 01-01
phase: 1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/main/java/cs120/lab06/App.java
  - src/main/resources/cs120/lab06/primary.fxml
autonomous: true
requirements_addressed:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
  - SETUP-05

must_haves:
  truths:
    - "mvn javafx:run from project root opens a blank JavaFX window with no console errors"
    - "mvn test completes without error (0 tests = acceptable; Surefire 3.1.2 is wired)"
    - "twister_words/ directory is present at project root with files 3.txt through 10.txt"
    - "No module-info.java exists anywhere under src/"
  artifacts:
    - path: "pom.xml"
      provides: "Maven build definition with JavaFX 21, JUnit 4.12, Surefire 3.1.2"
      contains: "maven-surefire-plugin version 3.1.2"
    - path: "src/main/java/cs120/lab06/App.java"
      provides: "JavaFX Application entry point"
    - path: "src/main/resources/cs120/lab06/primary.fxml"
      provides: "Minimal FXML stub for blank window"
  key_links:
    - from: "App.java"
      to: "primary.fxml"
      via: "App.class.getResource(\"primary.fxml\")"
      pattern: "getResource.*primary.fxml"
---

<objective>
Bootstrap the Maven project from the openjfx javafx-archetype-fxml archetype, delete module-info.java, pin the correct plugin versions, and verify that `mvn javafx:run` opens a blank window.

Purpose: This is the Walking Skeleton foundation. Nothing else in Phase 1 can run until Maven builds the project and the JavaFX window opens cleanly.

Output: A compilable Maven project with a blank JavaFX window, Surefire 3.1.2 wired, and no module-info.java.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scaffold-pure-java-model/01-CONTEXT.md
@.planning/phases/01-scaffold-pure-java-model/01-SKELETON.md

Phase 1 goal: Maven project builds and launches; WordDictionary and TwistController are fully implemented and unit-tested with zero JavaFX imports.

Critical decisions locked in (from CONTEXT.md):
- D-01: Generate with javafx-archetype-fxml (groupId=cs120, artifactId=lab06, JavaFX 21)
- D-02: Delete module-info.java immediately — JUnit 4.12 not module-aware
- D-03: Pin Surefire to 3.1.2 — 2.x silently skips JUnit 4 tests on JDK 21
- D-04: Keep archetype's App.java and primary.fxml as a blank-window stub for Phase 1
- twister_words/ is already present at the project root (3.txt through 10.txt) — do NOT move it
</context>

<tasks>

<task id="01-01-T01" name="Generate Maven scaffold, delete module-info.java, pin Surefire 3.1.2">
  <read_first>
    - CLAUDE.md — Critical implementation rules, scoring formula, run/test commands
    - .planning/research/STACK.md — pom.xml config examples (note: STACK.md suggests Surefire 2.22.2 but D-03 in CONTEXT.md overrides this; use 3.1.2)
    - .planning/research/PITFALLS.md — Pitfall 4 (missing javafx-controls), Pitfall 13 (Surefire version)
  </read_first>
  <action>
    Run the archetype generation command from the TextTwist project root directory. Because the project root is already a git repo with existing files (CLAUDE.md, twister_words/, etc.), do NOT create a subdirectory — generate into the current directory by passing -DoutputDirectory=. or by using the interactive archetype:generate flow with the correct parameters.

    Archetype command (run from /Users/andrewmcinnis/Desktop/TextTwist):

      mvn archetype:generate \
        -DarchetypeGroupId=org.openjfx \
        -DarchetypeArtifactId=javafx-archetype-fxml \
        -DarchetypeVersion=0.0.6 \
        -DgroupId=cs120 \
        -DartifactId=lab06 \
        -Dversion=1.0-SNAPSHOT \
        -Djavafx-version=21 \
        -DinteractiveMode=false

    If this creates a lab06/ subdirectory, move all generated files up to the project root (pom.xml, src/) and delete the empty lab06/ wrapper.

    After generation, make the following pom.xml edits:

    1. Verify dependencies include ALL THREE:
       - org.openjfx:javafx-controls:21
       - org.openjfx:javafx-fxml:21
       - junit:junit:4.12 (scope=test)

    2. In the build/plugins section, set javafx-maven-plugin to version 0.0.8 with mainClass set to cs120.lab06.App (NO module prefix — unnamed module after deleting module-info.java):

       &lt;mainClass&gt;cs120.lab06.App&lt;/mainClass&gt;

    3. Add or update the Surefire plugin entry to version 3.1.2 (per D-03 — overrides STACK.md's 2.22.2 recommendation):

       &lt;groupId&gt;org.apache.maven.plugins&lt;/groupId&gt;
       &lt;artifactId&gt;maven-surefire-plugin&lt;/artifactId&gt;
       &lt;version&gt;3.1.2&lt;/version&gt;

    Delete module-info.java (per D-02). It will be at src/main/java/module-info.java or src/main/java/cs120/lab06/module-info.java. Delete whichever exists.

    Do NOT touch twister_words/ — it is already at the project root where it must remain.
  </action>
  <acceptance_criteria>
    - pom.xml exists at the project root and contains groupId cs120 and artifactId lab06
    - pom.xml contains &lt;artifactId&gt;javafx-controls&lt;/artifactId&gt; with version 21
    - pom.xml contains &lt;artifactId&gt;javafx-fxml&lt;/artifactId&gt; with version 21
    - pom.xml contains &lt;artifactId&gt;junit&lt;/artifactId&gt; with version 4.12
    - pom.xml contains &lt;artifactId&gt;maven-surefire-plugin&lt;/artifactId&gt; with version 3.1.2
    - pom.xml mainClass value is cs120.lab06.App (no module prefix)
    - No file named module-info.java exists anywhere under src/ (find src/ -name module-info.java returns nothing)
    - src/main/java/cs120/lab06/App.java exists
    - src/main/resources/cs120/lab06/primary.fxml exists
    - twister_words/ directory still exists at project root with files 3.txt through 10.txt
    - mvn compile completes with BUILD SUCCESS
  </acceptance_criteria>
</task>

<task id="01-01-T02" name="Verify blank window launches via mvn javafx:run">
  <read_first>
    - pom.xml — Confirm mainClass is cs120.lab06.App and Surefire is 3.1.2 before running
    - src/main/java/cs120/lab06/App.java — Confirm it extends Application and calls primaryStage.show()
    - src/main/resources/cs120/lab06/primary.fxml — Confirm it has a valid root element that FXMLLoader can load
  </read_first>
  <action>
    Run `mvn javafx:run` from the project root (/Users/andrewmcinnis/Desktop/TextTwist). The blank window should open.

    If App.java loads primary.fxml via App.class.getResource("primary.fxml") and primary.fxml has a valid root node (the archetype generates a simple VBox or AnchorPane with a Label — leave it as-is), the window will open.

    If the window opens but shows a NullPointerException for FXML loading, check that the fx:controller attribute in primary.fxml matches the actual controller class path (cs120.lab06.PrimaryController or whatever the archetype generated). For Phase 1, the controller class name does not matter — just ensure FXML loads without errors.

    If mvn javafx:run fails because App.java references a controller class that does not exist, either:
    (a) Remove the fx:controller attribute from primary.fxml temporarily (safe for Phase 1 — no controller logic needed yet), or
    (b) Keep whatever class the archetype generated

    Do NOT rename PrimaryController.java to TwistController.java in Phase 1. The rename and wiring happen in Phase 2. Phase 1 goal is a blank window with no errors.

    Run `mvn test` as well — with no test files yet, Surefire 3.1.2 should report "Tests run: 0" (not a build failure).
  </action>
  <acceptance_criteria>
    - mvn javafx:run completes and a JavaFX window opens (window title does not matter)
    - No exceptions appear in the console output during window open (no NullPointerException, no ClassNotFoundException, no module-related errors)
    - The window is closeable without errors
    - mvn test completes with BUILD SUCCESS (0 tests run is acceptable at this stage)
    - No "WARNING: Unsupported JavaFX configuration" errors appear (if they do, verify javafx-maven-plugin version is 0.0.8)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → JVM | twister_words/ files read from project root; no user-supplied path at this stage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | twister_words/ path | accept | Dictionary files contain no sensitive data; word lists are educational content. Path is fixed at project root per spec. |
| T-01-02 | Denial of Service | mvn archetype:generate | accept | Dev-time only; no runtime attack surface. Low-value target for a CS lab. |
</threat_model>

<verification>
  <must_haves>
    <truth>mvn javafx:run from /Users/andrewmcinnis/Desktop/TextTwist opens a blank window with no console errors</truth>
    <truth>mvn test completes with BUILD SUCCESS</truth>
    <truth>find src/ -name module-info.java returns no results</truth>
    <truth>pom.xml contains maven-surefire-plugin version 3.1.2</truth>
    <truth>pom.xml mainClass is cs120.lab06.App with no module prefix</truth>
    <truth>twister_words/3.txt through twister_words/10.txt are present at project root</truth>
  </must_haves>
</verification>

<success_criteria>
Maven project compiles and runs as a blank JavaFX 21 window. Surefire 3.1.2 is wired. module-info.java is deleted. The project is ready for WordDictionary implementation in Plan 02.
</success_criteria>

<output>
After completion, create .planning/phases/01-scaffold-pure-java-model/01-01-SUMMARY.md documenting:
- The exact pom.xml dependency versions confirmed working
- Whether the archetype generated into a subdirectory (and whether files were moved up)
- Any deviations from the planned approach
- Console output snippet confirming mvn javafx:run success
</output>
