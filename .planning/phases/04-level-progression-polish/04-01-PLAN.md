---
phase: 04-level-progression-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/cs120/lab06/PrimaryController.java
autonomous: false
requirements:
  - PROG-02
  - PROG-04
  - PROG-05
  - PROG-06
  - VFX-01
  - VFX-02
  - VFX-03

must_haves:
  truths:
    - "Player can advance through all 8 levels when earning enough points"
    - "Player sees 'Advanced to Level N!' Alert before each new level starts"
    - "Player sees 'You Win!' Alert after completing Level 8"
    - "Player sees 'Game Over — not enough points.' Alert when threshold is not met"
    - "After any Alert is dismissed, the game continues or resets without manual intervention"
    - "Last Word button correctly restores the last-guessed word after a level advance"
    - "Valid-word and invalid-word flash feedback is preserved with correct playing guard"
  artifacts:
    - path: "src/main/java/cs120/lab06/PrimaryController.java"
      provides: "Complete level-progression PrimaryController"
      contains: "beginNextEpisode"
    - path: "src/main/java/cs120/lab06/PrimaryController.java"
      provides: "lastWordLetters field (List<String>)"
      contains: "lastWordLetters"
    - path: "src/main/java/cs120/lab06/PrimaryController.java"
      provides: "endEpisode routing with Alert and threshold"
      contains: "Advanced to Level"
  key_links:
    - from: "endEpisode()"
      to: "beginNextEpisode(letterCount)"
      via: "threshold check branch"
      pattern: "beginNextEpisode"
    - from: "endEpisode()"
      to: "startGame()"
      via: "game-over and game-won branches"
      pattern: "startGame"
    - from: "enterBtn handler"
      to: "lastWordLetters"
      via: "pressedButtons.stream().map(Button::getText).collect"
      pattern: "lastWordLetters"
---

<objective>
Rewrite PrimaryController.java to deliver complete 8-level progression: fix two known
bugs from Phase 3 (WR-01, WR-02), apply the CR-01 playing guard, add a
beginNextEpisode() private helper, and extend endEpisode() with score-threshold
evaluation, Alert dialogs for all three outcomes (advance / game-over / game-won),
and correct routing after each Alert is dismissed.

Purpose: Phase 3 left endEpisode() as a stub that stops the game with no routing.
This plan delivers the complete game loop — all 8 levels playable end to end.

Output: A fully updated PrimaryController.java implementing D-01 through D-09 from
04-CONTEXT.md and all pitfall mitigations from 04-RESEARCH.md.
</objective>

<execution_context>
@/Users/andrewmcinnis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewmcinnis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-level-progression-polish/04-CONTEXT.md
@.planning/phases/04-level-progression-polish/04-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- TwistController API used by PrimaryController. Extracted from TwistController.java. -->

public class TwistController {
    public TwistController(String folderPath)
    public void beginEpisode(int letterCount)      // resets levelScore + guessedWords; preserves cumulative score
    public int checkGuessWord(String wrd)          // returns n*n*10 or 0; adds to score and levelScore
    public List<String> shuffleLetters()
    public int getScore()                          // cumulative score
    public int getLevelScore()                     // per-episode score only
    public int getLevel()                          // DO NOT USE for display — use letterCount - 2 instead
    public String getTargetWord()
}

<!-- PrimaryController current state — read the file before modifying. Key gaps to fill: -->
<!-- Fields to add: int letterCount (initialized to 3), boolean targetWordGuessed, List<String> lastWordLetters -->
<!-- Fields to remove: List<Button> lastWordButtons -->
<!-- Methods to add: private void beginNextEpisode(int newLetterCount) -->
<!-- Methods to change: startGame(), endEpisode(), enterBtn handler, lastWordBtn handler -->
<!-- Methods to add import: javafx.scene.control.Alert -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bug fixes — WR-01 (lastWordLetters), WR-02 (startGame guards), CR-01 (playing guard), extract beginNextEpisode()</name>

  <read_first>
    src/main/java/cs120/lab06/PrimaryController.java
    .planning/phases/04-level-progression-polish/04-CONTEXT.md
    .planning/phases/04-level-progression-polish/04-RESEARCH.md
  </read_first>

  <files>src/main/java/cs120/lab06/PrimaryController.java</files>

  <action>
Make all of the following changes to PrimaryController.java in a single edit pass.
Do NOT add fenced code blocks or full file dumps in this action — implement directly.

1. ADD IMPORT: Add "import javafx.scene.control.Alert;" to the import block.

2. REPLACE FIELD: Remove "private List&lt;Button&gt; lastWordButtons = new ArrayList&lt;&gt;();" and add
   two fields in its place:
   - "private List&lt;String&gt; lastWordLetters = new ArrayList&lt;&gt;();"
   - "private int letterCount = 3;"
   - "private boolean targetWordGuessed = false;"

3. ADD METHOD beginNextEpisode(int newLetterCount): This private void method is the shared
   reset helper called by startGame() (for full reset) and the level-advance path (per D-04).
   It must perform all of the following in order:
   - if (gameTimeline != null) gameTimeline.stop();   [Pitfall 4 — double Timeline guard]
   - letterCount = newLetterCount;
   - twistController.beginEpisode(letterCount);
   - playing = true;
   - targetWordGuessed = false;
   - secondsLeft = 120;
   - pressedButtons.clear();
   - lastWordLetters.clear();
   - foundWords.clear();
   - guessDisplay.getChildren().clear();
   - letterButtonArea.getChildren().clear();
   - lastWordBtn.setDisable(true);
   - twistBtn.setDisable(false);
   - enterBtn.setDisable(false);
   - clearBtn.setDisable(false);
   - timeLabel.setText("2:00");
   - timeLabel.getStyleClass().removeAll("timer-warning", "timer-critical");
   - levelLabel.setText("Level: " + (letterCount - 2));
   - buildLetterButtons();
   - gameTimeline = new Timeline(new KeyFrame(Duration.seconds(1), e -&gt; onTick()));
   - gameTimeline.setCycleCount(Timeline.INDEFINITE);
   - gameTimeline.play();

4. REWRITE startGame() (WR-02 fix, per D-08): Remove the existing startGame() body.
   New body:
   - if (gameTimeline != null) gameTimeline.stop();
   - twistController = new TwistController("twister_words");
   - scoreLabel.setText("Score: 0");
   - beginNextEpisode(3);
   The setOnAction wiring for clearBtn, twistBtn, enterBtn, and lastWordBtn moves to
   initialize() (see step 6) so handlers are registered once, not re-registered on every
   restart (per IN-02 recommendation in 04-RESEARCH.md Open Questions).

5. MOVE HANDLERS to initialize(): Move all four setOnAction blocks (clearBtn, twistBtn,
   enterBtn, lastWordBtn) from startGame() to the end of initialize(), after the existing
   foundWordsList.setItems(foundWords) line and before the startGame() call.

   clearBtn handler body (unchanged from Phase 3):
   - if (!playing) return;
   - for (Node node : letterButtonArea.getChildren()) { ((Button) node).setDisable(false); }
   - pressedButtons.clear();
   - guessDisplay.getChildren().clear();

   twistBtn handler body (unchanged):
   - if (!playing) return;
   - pressedButtons.clear();
   - guessDisplay.getChildren().clear();
   - letterButtonArea.getChildren().clear();
   - buildLetterButtons();
   - lastWordBtn.setDisable(true);

   enterBtn handler body — CHANGE: replace "lastWordButtons = new ArrayList&lt;&gt;(pressedButtons);"
   with "lastWordLetters = pressedButtons.stream().map(Button::getText).collect(Collectors.toList());"
   (WR-01 fix, per D-07). Also set targetWordGuessed = true before calling endEpisode() in the
   target-word-guessed branch.
   Full valid-path:
   - foundWords.add(guessWord);
   - scoreLabel.setText("Score: " + twistController.getScore());
   - lastWordLetters = pressedButtons.stream().map(Button::getText).collect(Collectors.toList());
   - lastWordBtn.setDisable(false);
   - if (guessWord.equalsIgnoreCase(twistController.getTargetWord())) {
       targetWordGuessed = true;
       endEpisode();
       return;
     }
   - guessDisplay.getStyleClass().add("guess-valid");
   - PauseTransition pause = new PauseTransition(Duration.millis(500));
   - pause.setOnFinished(e2 -&gt; {
       guessDisplay.getStyleClass().remove("guess-valid");
       pressedButtons.clear();
       guessDisplay.getChildren().clear();
       if (playing) { letterButtonArea.getChildren().forEach(n -&gt; ((Button) n).setDisable(false)); }
     });
   - pause.play();
   Invalid-path PauseTransition onFinished CR-01 guard:
   - guessDisplay.getStyleClass().remove("guess-invalid");
   - pressedButtons.clear();
   - guessDisplay.getChildren().clear();
   - if (playing) { letterButtonArea.getChildren().forEach(n -&gt; ((Button) n).setDisable(false)); }

   lastWordBtn handler body — REWRITE (WR-01 fix, per D-07):
   - guessDisplay.getChildren().clear();
   - pressedButtons.clear();
   - List&lt;Node&gt; available = new ArrayList&lt;&gt;(letterButtonArea.getChildren());
   - for (String letter : lastWordLetters) {
       for (Node node : available) {
         Button btn = (Button) node;
         if (!btn.isDisabled() &amp;&amp; btn.getText().equals(letter)) {
           handleLetterButton(btn);
           available.remove(btn);
           break;
         }
       }
     }
   Note: iterate `available` (a separate mutable list copy), not `letterButtonArea.getChildren()`
   directly — avoids ConcurrentModificationException.

6. Remove the redundant twistController.beginEpisode(3) and playing = true and secondsLeft = 120
   lines that were at the top of the old startGame() body — beginNextEpisode() handles those.
  </action>

  <verify>
    <automated>grep -n "lastWordLetters" src/main/java/cs120/lab06/PrimaryController.java</automated>
    <automated>grep -n "lastWordButtons" src/main/java/cs120/lab06/PrimaryController.java</automated>
    <automated>grep -n "beginNextEpisode" src/main/java/cs120/lab06/PrimaryController.java</automated>
    <automated>grep -n "if (playing)" src/main/java/cs120/lab06/PrimaryController.java</automated>
    <automated>mvn compile -q 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - grep output for "lastWordLetters" shows at least 3 lines (field declaration, store in enterBtn, use in lastWordBtn)
    - grep output for "lastWordButtons" shows zero lines (field is fully removed)
    - grep output for "beginNextEpisode" shows at least 2 lines (method definition, call from startGame())
    - grep output for "if (playing)" shows at least 2 lines (both PauseTransition onFinished guards)
    - mvn compile exits with BUILD SUCCESS and zero errors
  </acceptance_criteria>

  <done>PrimaryController.java compiles cleanly; lastWordButtons field is gone; lastWordLetters, letterCount, targetWordGuessed fields present; beginNextEpisode() method exists; startGame() delegates to beginNextEpisode(3); both PauseTransition blocks guard "if (playing)" before re-enabling buttons.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend endEpisode() — threshold evaluation, Alert routing, win/game-over/advance outcomes</name>

  <read_first>
    src/main/java/cs120/lab06/PrimaryController.java
    .planning/phases/04-level-progression-polish/04-CONTEXT.md
  </read_first>

  <files>src/main/java/cs120/lab06/PrimaryController.java</files>

  <action>
Replace the body of endEpisode() with the full routing logic. The existing stub stops
the game and disables controls — keep those lines, then add everything below them.

Current endEpisode() stub body (preserve these lines):
- playing = false;
- gameTimeline.stop();
- twistBtn.setDisable(true);
- enterBtn.setDisable(true);
- lastWordBtn.setDisable(true);
- clearBtn.setDisable(true);
- letterButtonArea.getChildren().forEach(n -&gt; ((Button) n).setDisable(true));

ADD after the last line above:

1. Compute threshold:
   int threshold = (int)(0.25 * letterCount * letterCount * 10);
   boolean thresholdMet = twistController.getLevelScore() &gt;= threshold;

2. Win condition check (letterCount == 10 AND (targetWordGuessed OR thresholdMet)):
   Per PROG-06, D-05, and 04-RESEARCH.md Pitfall 3.
   - Show Alert: title="TextTwist", headerText=null, contentText="You Win!"
   - alert.showAndWait();
   - startGame();

3. Advance check (thresholdMet AND letterCount &lt; 10):
   Per PROG-02, PROG-04, D-03, D-04.
   - letterCount++ (increment BEFORE formatting the message — avoids Pitfall 2)
   - int newLevel = letterCount - 2;
   - Show Alert: contentText = "Advanced to Level " + newLevel + "!"
   - alert.showAndWait();
   - beginNextEpisode(letterCount);

4. Game-over fallthrough (threshold not met, not at level 8):
   Per PROG-05, D-05, D-06.
   - Show Alert: contentText = "Game Over — not enough points."
   - alert.showAndWait();
   - startGame();

For all three Alerts: Alert.AlertType.INFORMATION, title "TextTwist", setHeaderText(null).

Alert format (all three):
  Alert alert = new Alert(Alert.AlertType.INFORMATION);
  alert.setTitle("TextTwist");
  alert.setHeaderText(null);
  alert.setContentText("...");
  alert.showAndWait();

Exact message strings per D-02:
- Advance:    "Advanced to Level " + newLevel + "!"
- Game over:  "Game Over — not enough points."
- Game won:   "You Win!"

Note: The em dash in "Game Over — not enough points." is a Unicode em dash character
(U+2014), not two hyphens. Use the literal character in the string.
  </action>

  <verify>
    <automated>grep -n "Advanced to Level" src/main/java/cs120/lab06/PrimaryController.java</automated>
    <automated>grep -n "Game Over" src/main/java/cs120/lab06/PrimaryController.java</automated>
    <automated>grep -n "You Win" src/main/java/cs120/lab06/PrimaryController.java</automated>
    <automated>grep -n "beginNextEpisode\|startGame" src/main/java/cs120/lab06/PrimaryController.java | grep -v "//"</automated>
    <automated>mvn compile -q 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - grep "Advanced to Level" shows at least 1 line inside endEpisode()
    - grep "Game Over" shows the exact string "Game Over — not enough points." (em dash, not double hyphen)
    - grep "You Win" shows at least 1 line
    - grep for "beginNextEpisode|startGame" (non-comment) shows calls inside endEpisode() body
    - mvn compile exits with BUILD SUCCESS and zero errors
    - levelLabel.setText lines reference "(letterCount - 2)" via beginNextEpisode (not a hardcoded number)
  </acceptance_criteria>

  <done>endEpisode() contains three branches: win (letterCount==10 AND (targetWordGuessed||thresholdMet)), advance (thresholdMet), game-over (else); each branch shows a correctly-worded Alert via showAndWait() then routes to beginNextEpisode(letterCount) or startGame() as appropriate; mvn compile BUILD SUCCESS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full 8-level game loop in PrimaryController.java: WR-01 (lastWordLetters), WR-02
    (startGame guards), CR-01 (playing guard), beginNextEpisode() helper, and
    endEpisode() with threshold evaluation and Alert routing. All 7 Phase 4 requirements
    are now in code.
  </what-built>
  <how-to-verify>
    Run from the project root (dictionary path depends on cwd):

      mvn javafx:run

    Manual verification checklist:

    1. GAME START — Level 1 (3-letter words) loads. Score: 0, Level: 1, timer at 2:00.

    2. ADVANCE path — submit valid words until levelScore >= floor(0.25 * 9 * 10) = 22 pts
       (one 3-letter word = 90 pts, so any valid 3-letter word exceeds the threshold).
       Let the timer expire (or guess the target word).
       Expected: Alert appears with "Advanced to Level 2!", clicking OK starts Level 2
       (4-letter words, timer resets to 2:00, score preserved).

    3. LAST WORD after advance — submit a valid word at Level 2, then click "Last Word".
       Expected: the same letters reappear in the guess display correctly (not blank,
       not from a button that no longer exists).

    4. GAME OVER path — let the timer expire at a level with 0 points scored.
       Expected: Alert "Game Over — not enough points.", clicking OK resets to Level 1
       with Score: 0.

    5. VALID/INVALID flash — submit a valid word (not target word). Expected: green flash
       on guess display, letters clear, buttons re-enable after ~500ms.
       Submit an invalid string. Expected: red flash, same clear-and-re-enable.
       After flash: enter/clear/twist buttons are functional (not stuck disabled).

    6. WIN path (manual shortcut — reach Level 8 by earning enough points at each level,
       OR inspect endEpisode() logic manually if full playthrough is too slow).
       If testing manually: at letterCount==10 with thresholdMet OR targetWordGuessed,
       expected Alert text is "You Win!", then game resets to Level 1.

    7. TIMER DISPLAY — timer shows mm:ss, turns warning CSS class at 20s, critical at 10s.
       Verify timeLabel CSS classes are not accumulating across levels.
  </how-to-verify>
  <resume-signal>Type "approved" if all paths work correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → game model | Player-entered guess word (assembled from letter buttons) passed to checkGuessWord() |
| File system → dictionary | Dictionary files read via new File() at application startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | PrimaryController — double Timeline on level advance | mitigate | beginNextEpisode() guards: if (gameTimeline != null) gameTimeline.stop() before creating new Timeline |
| T-04-02 | Denial of Service | PrimaryController — PauseTransition fires after endEpisode() | mitigate | Both PauseTransition onFinished blocks guard "if (playing)" before re-enabling buttons (CR-01) |
| T-04-03 | Information Disclosure | TwistController.getLevel() returns stale value | mitigate | Do not use getLevel() for display; use letterCount - 2 exclusively (D-09) |
| T-04-04 | Tampering | lastWordBtn handler — ConcurrentModificationException on available list | mitigate | Iterate a mutable copy (new ArrayList<>(letterButtonArea.getChildren())), not the live ObservableList |
| T-04-05 | Elevation of Privilege | checkGuessWord() — cumulative score used for threshold | accept | Threshold compared against getLevelScore() (per-episode only); getScore() (cumulative) is only used for display |
</threat_model>

<verification>
## Phase 4 Verification

### Automated (run after Task 2 completes)

```bash
# File must compile cleanly
mvn compile -q && echo "COMPILE OK"

# Field audit — required fields present
grep -c "lastWordLetters" src/main/java/cs120/lab06/PrimaryController.java
grep -c "letterCount" src/main/java/cs120/lab06/PrimaryController.java
grep -c "targetWordGuessed" src/main/java/cs120/lab06/PrimaryController.java
grep -c "beginNextEpisode" src/main/java/cs120/lab06/PrimaryController.java

# Bug fix confirmation — old field gone
grep -c "lastWordButtons" src/main/java/cs120/lab06/PrimaryController.java
# Expected: 0

# Alert strings present
grep -c "Advanced to Level" src/main/java/cs120/lab06/PrimaryController.java
grep -c "Game Over" src/main/java/cs120/lab06/PrimaryController.java
grep -c "You Win" src/main/java/cs120/lab06/PrimaryController.java

# No inline setStyle() calls (CLAUDE.md rule 6)
grep -v '^[[:space:]]*//' src/main/java/cs120/lab06/PrimaryController.java | grep -c "setStyle("
# Expected: 0

# TwistController still zero JavaFX imports (CLAUDE.md rule 3)
grep "import javafx" src/main/java/cs120/lab06/TwistController.java
# Expected: no output
```

### Manual (checkpoint:human-verify task)

See Task 3 verification checklist: advance, game-over, win, Last Word, valid/invalid flash.
</verification>

<success_criteria>
1. mvn compile reports BUILD SUCCESS with zero errors or warnings
2. mvn javafx:run opens Level 1; playing through two levels with sufficient points shows
   "Advanced to Level 2!" Alert and starts Level 2 with preserved score
3. Scoring below the threshold at any level shows "Game Over — not enough points." and
   resets to Level 1 with Score: 0
4. Completing Level 8 (letterCount==10) shows "You Win!" and resets to Level 1
5. Last Word button correctly restores guessed letters after a level advance
6. Valid-word and invalid-word flash feedback (green/red) works correctly and controls
   re-enable after the flash; no stuck-disabled state
7. No inline setStyle() calls exist anywhere in PrimaryController.java
8. TwistController.java has zero JavaFX imports
9. java.util.Timer is not used anywhere (Timeline only)
</success_criteria>

<output>
After completion, create `.planning/phases/04-level-progression-polish/04-01-SUMMARY.md`
using the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
