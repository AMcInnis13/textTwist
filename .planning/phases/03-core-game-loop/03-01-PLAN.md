---
phase: 03-core-game-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/cs120/lab06/styles.css
autonomous: true
requirements:
  - CTRL-05

must_haves:
  truths:
    - "styles.css contains a .guess-valid class with dark green background and green border"
    - "styles.css contains a .guess-invalid class with dark red background and red border"
    - "styles.css does NOT contain a .guess-correct class"
  artifacts:
    - path: "src/main/resources/cs120/lab06/styles.css"
      provides: "Feedback CSS classes for guess display"
      contains: ".guess-valid"
  key_links:
    - from: "PrimaryController.java (Phase 3 Wave 2)"
      to: "guessDisplay HBox"
      via: "getStyleClass().add(\"guess-valid\") / getStyleClass().add(\"guess-invalid\")"
      pattern: "guess-valid|guess-invalid"
---

<objective>
Update styles.css to provide the two feedback classes PrimaryController will toggle on the guessDisplay HBox during Enter handling. The existing `.guess-correct` stub (border-only, no background) must be renamed to `.guess-valid` and expanded with a dark green background fill. The existing `.guess-invalid` stub must be expanded with a dark red background fill.

Purpose: Wave 2 (PrimaryController implementation) calls `guessDisplay.getStyleClass().add("guess-valid")` and `guessDisplay.getStyleClass().add("guess-invalid")`. Those class names must exist in CSS before the controller is wired or the visual feedback will be silently absent at runtime.

Output: Updated styles.css with `.guess-valid` and `.guess-invalid` in Section 9; `.guess-correct` removed.
</objective>

<execution_context>
@/Users/andrewmcinnis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewmcinnis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/PROJECT.md
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/ROADMAP.md
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/STATE.md
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/phases/03-core-game-loop/03-CONTEXT.md
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/phases/03-core-game-loop/03-UI-SPEC.md

<interfaces>
<!-- Current Section 9 state in styles.css (lines 165-174) — these are the rules being replaced. -->

Current:
  .guess-correct { -fx-border-color: #43a047; -fx-border-width: 2; }
  .guess-invalid  { -fx-border-color: #e53935; -fx-border-width: 2; }

Target (per D-13 and UI-SPEC Color Contract):
  .guess-valid   { -fx-background-color: #1b5e20; -fx-border-color: #43a047; -fx-border-width: 2; }
  .guess-invalid { -fx-background-color: #b71c1c; -fx-border-color: #e53935; -fx-border-width: 2; }

Note: guessDisplay HBox also carries the permanent .guess-display class which is never removed.
The feedback class is added on top of it; only the specific feedback class is removed by PauseTransition.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Section 9 feedback CSS rules</name>
  <files>src/main/resources/cs120/lab06/styles.css</files>
  <read_first>
    src/main/resources/cs120/lab06/styles.css — read before editing to see the exact current Section 9 text that must be replaced (lines ~162–174, containing .guess-correct and .guess-invalid stubs)
  </read_first>
  <action>
    In styles.css Section 9 (headed "Visual feedback classes for guess display"), replace the entire block that currently contains .guess-correct and .guess-invalid with the two corrected rules below. No other sections are touched.

    Replace the .guess-correct rule (border-only, class name incorrect) with a .guess-valid rule:
      selector: .guess-valid
      -fx-background-color: #1b5e20
      -fx-border-color: #43a047
      -fx-border-width: 2

    Update the .guess-invalid rule to add a background fill (keep the existing border declarations):
      selector: .guess-invalid
      -fx-background-color: #b71c1c
      -fx-border-color: #e53935
      -fx-border-width: 2

    Update the section comment to read "guess-valid" and "guess-invalid" (remove any reference to "guess-correct"). The section header comment text and surrounding blank lines must remain intact so the 11-section structure of the file is preserved.

    Do NOT use setStyle() or modify any Java source file. Do NOT change any other CSS section.
  </action>
  <verify>
    <automated>grep -c "guess-valid" /Users/andrewmcinnis/Desktop/TextTwist/src/main/resources/cs120/lab06/styles.css</automated>
    <automated>grep -c "guess-correct" /Users/andrewmcinnis/Desktop/TextTwist/src/main/resources/cs120/lab06/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - styles.css contains the selector ".guess-valid" (grep -c returns >= 1)
    - styles.css does NOT contain the selector ".guess-correct" (grep -c returns 0)
    - styles.css contains "-fx-background-color: #1b5e20" under .guess-valid
    - styles.css contains "-fx-background-color: #b71c1c" under .guess-invalid
    - styles.css contains "-fx-border-color: #43a047" under .guess-valid
    - styles.css contains "-fx-border-color: #e53935" under .guess-invalid
    - styles.css still contains all 11 section header comments (no other sections altered)
    - No Java source file was modified (git diff --name-only shows only styles.css)
  </acceptance_criteria>
  <done>Section 9 of styles.css defines .guess-valid (green fill + border) and .guess-invalid (red fill + border); .guess-correct is gone; no other section is altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file → JavaFX renderer | Static file read at stylesheet load; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-CSS-01 | Tampering | styles.css | accept | CSS is a static presentation file; no secrets, no user data; file integrity is enforced by version control |
</threat_model>

<verification>
After the task completes:

1. Run: grep "guess-valid" src/main/resources/cs120/lab06/styles.css — must print the .guess-valid selector line
2. Run: grep "guess-correct" src/main/resources/cs120/lab06/styles.css — must produce no output (exit 1)
3. Run: grep -c "SECTION" src/main/resources/cs120/lab06/styles.css — must return 11 (all section headers intact)
4. Run: mvn javafx:run — game window opens without CSS parse errors in console (no "Could not load stylesheet" warnings)
</verification>

<success_criteria>
styles.css Section 9 contains exactly .guess-valid and .guess-invalid with both -fx-background-color and -fx-border-color declared. The class name .guess-correct does not appear anywhere in the file. All other CSS sections are unchanged.
</success_criteria>

<output>
After completion, create .planning/phases/03-core-game-loop/03-01-SUMMARY.md
</output>
