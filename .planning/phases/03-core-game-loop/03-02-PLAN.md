---
phase: 03-core-game-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/main/java/cs120/lab06/PrimaryController.java
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - CTRL-04
  - CTRL-05
  - CTRL-06
  - CTRL-07
  - TIMER-01
  - TIMER-02
  - TIMER-03
  - TIMER-04
  - TIMER-05
  - PROG-01
  - PROG-03
  - PROG-07
  - PROG-08

must_haves:
  truths:
    - "Game starts automatically on window open — letter buttons appear, timer shows 2:00 and counts down"
    - "Clicking a letter button appends a gold tile to guessDisplay and disables that button"
    - "Clear re-enables all letter buttons and empties guessDisplay"
    - "Twist replaces letter buttons with newly shuffled order, clears guess, and disables Last Word"
    - "Enter on a valid word adds it to the found-words list, updates score label, and flashes green for 500ms"
    - "Enter on an invalid or duplicate word flashes red for 500ms and clears the guess"
    - "Enter on the exact target word immediately ends the episode without the flash sequence"
    - "Last Word is disabled until first valid word; clicking it restores that guess into the display"
    - "Timer label shows mm:ss, turns amber at <= 20s, turns red at <= 10s"
    - "Timer expiry calls endEpisode() — stops Timeline and disables all controls"
    - "Score and levelScore update correctly: n*n*10 points per valid n-letter word"
  artifacts:
    - path: "src/main/java/cs120/lab06/PrimaryController.java"
      provides: "Full game loop controller wired to FXML"
      exports: ["initialize", "startGame", "buildLetterButtons", "onTick", "endEpisode"]
    - path: "src/main/resources/cs120/lab06/styles.css"
      provides: ".guess-valid and .guess-invalid feedback classes (from Plan 01)"
      contains: ".guess-valid"
  key_links:
    - from: "PrimaryController.initialize()"
      to: "startGame()"
      via: "direct call after foundWords binding"
      pattern: "startGame\\(\\)"
    - from: "Timeline KeyFrame"
      to: "onTick()"
      via: "lambda or method reference in KeyFrame handler"
      pattern: "onTick|Duration.seconds"
    - from: "endEpisode()"
      to: "gameTimeline.stop()"
      via: "direct call inside endEpisode"
      pattern: "gameTimeline\\.stop"
    - from: "Enter handler"
      to: "endEpisode()"
      via: "target word match check before PauseTransition"
      pattern: "endEpisode"
---

<objective>
Implement the complete game loop in PrimaryController. After this plan, a player can open the app, click letter buttons to build a guess, submit words, earn points, use Clear/Twist/Last Word, and race the 120-second Timeline timer — a fully playable single episode at Level 1 (3-letter words).

Purpose: This is the MVP slice that delivers the Phase 3 goal. All eight interaction contracts (letter click, Clear, Twist, Enter valid, Enter invalid, Last Word, Timeline tick, endEpisode) are wired and guarded by `boolean playing`.

Output: PrimaryController.java with all fields, helpers, handlers, and Timeline implemented. No other file is modified.
</objective>

<execution_context>
@/Users/andrewmcinnis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewmcinnis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/PROJECT.md
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/ROADMAP.md
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/STATE.md
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/phases/03-core-game-loop/03-CONTEXT.md
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/phases/03-core-game-loop/03-UI-SPEC.md
@/Users/andrewmcinnis/Desktop/TextTwist/.planning/phases/03-core-game-loop/03-01-SUMMARY.md

<interfaces>
<!-- TwistController public API — do NOT modify TwistController.java -->
<!-- Source: src/main/java/cs120/lab06/TwistController.java -->

Constructor:
  TwistController(String folderPath)  // e.g. new TwistController("twister_words")

Methods:
  void beginEpisode(int letterCount)   // loads word file, picks random targetWord, resets levelScore + guessedWords
  int  checkGuessWord(String wrd)      // returns n*n*10 for valid non-duplicate word; 0 otherwise; adds to score/levelScore internally
  List<String> shuffleLetters()        // returns shuffled list of single-char strings from targetWord
  String getTargetWord()               // returns current target word (uppercase)
  int    getScore()                    // cumulative score across episodes
  int    getLevelScore()               // per-episode score
  int    getLevel()                    // current level number (starts at 1)

<!-- FXML fields already declared in PrimaryController (Phase 2) -->
<!-- Source: src/main/java/cs120/lab06/PrimaryController.java -->

@FXML private ListView<String> foundWordsList;
@FXML private FlowPane         letterButtonArea;
@FXML private HBox             guessDisplay;
@FXML private Label            scoreLabel;
@FXML private Label            levelLabel;
@FXML private Label            timeLabel;
@FXML private Button           twistBtn;
@FXML private Button           enterBtn;
@FXML private Button           lastWordBtn;
@FXML private Button           clearBtn;

private ObservableList<String> foundWords;   // bound to foundWordsList in existing initialize()

<!-- CSS classes applied by PrimaryController (never via setStyle()) -->
<!-- Letter button: "letter-btn" added in buildLetterButtons() -->
<!-- Letter tile:   "letter-slot" added when creating Label children of guessDisplay -->
<!-- Timer states:  "timer-warning" (<=20s), "timer-critical" (<=10s) — removeAll both before adding -->
<!-- Guess feedback: "guess-valid" (500ms flash), "guess-invalid" (500ms flash) — add then remove in PauseTransition -->
<!-- guessDisplay permanent class "guess-display" is in FXML — never remove it -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add instance fields and startGame() / buildLetterButtons() infrastructure</name>
  <files>src/main/java/cs120/lab06/PrimaryController.java</files>
  <read_first>
    src/main/java/cs120/lab06/PrimaryController.java — read the entire file before editing; it has the Phase 2 skeleton that must be extended, not replaced
    src/main/java/cs120/lab06/TwistController.java — skim to confirm method signatures match what is described in the interfaces block above
    CLAUDE.md — re-read rules: Timeline not Timer; no setStyle(); track buttons by object ref; TwistController zero JavaFX imports (do not modify it); single endEpisode() entry point
  </read_first>
  <action>
    Add the following non-FXML instance fields to PrimaryController (after the existing `foundWords` declaration):

    - private TwistController twistController   (initialized in startGame, not at declaration)
    - private Timeline gameTimeline             (initialized in startGame)
    - private List&lt;Button&gt; pressedButtons = new ArrayList&lt;&gt;()   (tracks current guess in press order; D-01)
    - private List&lt;Button&gt; lastWordButtons = new ArrayList&lt;&gt;()   (snapshot of pressedButtons after successful Enter; D-09)
    - private int secondsLeft                   (initialized to 120 in startGame)
    - private boolean playing                   (initialized to true in startGame; D-22)

    Add required imports (only what is needed for these fields; no TwistController imports in wrong file):
      javafx.animation.Timeline, javafx.animation.KeyFrame, javafx.animation.PauseTransition,
      javafx.util.Duration, java.util.ArrayList, java.util.List, java.util.stream.Collectors

    Update initialize() to call startGame() after the existing ObservableList binding:
      foundWords = FXCollections.observableArrayList();
      foundWordsList.setItems(foundWords);
      startGame();    // D-20

    Implement private void startGame():
      1. twistController = new TwistController("twister_words")   (per D-21; folder path relative to working directory)
      2. twistController.beginEpisode(3)                          (Level 1 = 3 letters; per D-20)
      3. playing = true
      4. secondsLeft = 120
      5. timeLabel.setText("2:00")
      6. scoreLabel.setText("Score: 0")
      7. levelLabel.setText("Level: 1")                          (static in Phase 3; per D-23)
      8. buildLetterButtons()
      9. gameTimeline = new Timeline(new KeyFrame(Duration.seconds(1), e -&gt; onTick()))
      10. gameTimeline.setCycleCount(Timeline.INDEFINITE)
      11. gameTimeline.play()

    Implement private void buildLetterButtons():
      1. letterButtonArea.getChildren().clear()   (safe to call even if empty; D-07)
      2. List&lt;String&gt; letters = twistController.shuffleLetters()
      3. For each letter string in letters:
         a. Button btn = new Button(letter)
         b. btn.getStyleClass().add("letter-btn")
         c. btn.setOnAction(e -&gt; handleLetterButton(btn))   (capture btn by reference; D-05)
         d. letterButtonArea.getChildren().add(btn)

    Do NOT inline the letter handler body here. The handler is implemented in Task 2. The lambda just calls handleLetterButton(btn).

    Verify the file compiles before proceeding to Task 2:
      mvn compile -q from the project root must exit 0.
  </action>
  <verify>
    <automated>cd /Users/andrewmcinnis/Desktop/TextTwist && mvn compile -q 2>&amp;1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - mvn compile -q exits 0 (no compilation errors)
    - PrimaryController.java contains "private Timeline gameTimeline"
    - PrimaryController.java contains "private List&lt;Button&gt; pressedButtons"
    - PrimaryController.java contains "private List&lt;Button&gt; lastWordButtons"
    - PrimaryController.java contains "private boolean playing"
    - PrimaryController.java contains "private int secondsLeft"
    - PrimaryController.java contains "startGame()" called from initialize()
    - PrimaryController.java contains "twistController = new TwistController(\"twister_words\")"
    - PrimaryController.java contains "gameTimeline.setCycleCount(Timeline.INDEFINITE)"
    - PrimaryController.java contains "buildLetterButtons()"
    - PrimaryController.java contains "btn.getStyleClass().add(\"letter-btn\")"
    - No setStyle() call appears anywhere in PrimaryController.java
    - TwistController.java is unmodified (git diff --name-only shows only PrimaryController.java)
  </acceptance_criteria>
  <done>PrimaryController compiles with all fields and infrastructure methods in place; letter buttons are created dynamically but handlers are stubs pending Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Implement all event handlers, onTick, and endEpisode</name>
  <files>src/main/java/cs120/lab06/PrimaryController.java</files>
  <read_first>
    src/main/java/cs120/lab06/PrimaryController.java — read after Task 1 edits; use the current file state
    .planning/phases/03-core-game-loop/03-UI-SPEC.md — read Interaction Contracts sections A through G for the exact step-by-step sequence for each handler; these are the source of truth for ordering (e.g. score updates before PauseTransition, target word check before flash, removeAll on timer before add)
    CLAUDE.md — re-confirm: single endEpisode() entry point; CSS class accumulation pitfall on timeLabel; no inline setStyle()
  </read_first>
  <action>
    Implement the following private methods in PrimaryController. Each method is described by its exact behavioral contract. Do not deviate from the step ordering — the ordering is load-bearing (e.g., score must update before PauseTransition, target word match must be checked before starting the pause).

    --- private void handleLetterButton(Button btn) ---
    Guard: if (!playing) return immediately.
    1. btn.setDisable(true)
    2. pressedButtons.add(btn)
    3. Label tile = new Label(btn.getText())
    4. tile.getStyleClass().add("letter-slot")
    5. guessDisplay.getChildren().add(tile)

    --- Wire clearBtn.setOnAction in startGame() (or initialize()) ---
    Guard: if (!playing) return immediately.
    1. For each node in letterButtonArea.getChildren(): ((Button) node).setDisable(false)
    2. pressedButtons.clear()
    3. guessDisplay.getChildren().clear()

    --- Wire twistBtn.setOnAction ---
    Guard: if (!playing) return immediately.
    1. pressedButtons.clear()
    2. guessDisplay.getChildren().clear()
    3. letterButtonArea.getChildren().clear()
    4. buildLetterButtons()        (creates new Button objects; fresh shuffle)
    5. lastWordBtn.setDisable(true)

    --- Wire enterBtn.setOnAction ---
    Guard: if (!playing) return immediately.
    If pressedButtons is empty: return immediately (no feedback; per UI-SPEC edge case table).
    1. String guessWord = pressedButtons.stream().map(Button::getText).collect(Collectors.joining())
    2. int points = twistController.checkGuessWord(guessWord)

    If points &gt; 0 (valid word):
      3. foundWords.add(guessWord)
      4. scoreLabel.setText("Score: " + twistController.getScore())
      5. lastWordButtons = new ArrayList&lt;&gt;(pressedButtons)
      6. lastWordBtn.setDisable(false)
      7. if (guessWord.equalsIgnoreCase(twistController.getTargetWord())) { endEpisode(); return; }
         (target word matched — end episode immediately, skip flash; per D-18)
      8. guessDisplay.getStyleClass().add("guess-valid")
      9. PauseTransition pause = new PauseTransition(Duration.millis(500))
      10. pause.setOnFinished(e2 -&gt; {
            guessDisplay.getStyleClass().remove("guess-valid")    // remove only this class; keep .guess-display
            pressedButtons.clear()
            guessDisplay.getChildren().clear()
            letterButtonArea.getChildren().forEach(n -&gt; ((Button) n).setDisable(false))
          })
      11. pause.play()

    If points == 0 (invalid or duplicate):
      3. guessDisplay.getStyleClass().add("guess-invalid")
      4. PauseTransition pause = new PauseTransition(Duration.millis(500))
      5. pause.setOnFinished(e2 -&gt; {
            guessDisplay.getStyleClass().remove("guess-invalid")
            pressedButtons.clear()
            guessDisplay.getChildren().clear()
            letterButtonArea.getChildren().forEach(n -&gt; ((Button) n).setDisable(false))
          })
      6. pause.play()

    --- Wire lastWordBtn.setOnAction ---
    No playing guard needed in body (button is disabled when !playing via endEpisode).
    1. pressedButtons = new ArrayList&lt;&gt;(lastWordButtons)
    2. for each Button btn in pressedButtons: btn.setDisable(true)
    3. guessDisplay.getChildren().clear()
    4. for each Button btn in pressedButtons:
         Label tile = new Label(btn.getText())
         tile.getStyleClass().add("letter-slot")
         guessDisplay.getChildren().add(tile)

    --- private void onTick() ---
    (Called by Timeline KeyFrame every 1 second; fires on FX thread — direct label updates are safe.)
    1. secondsLeft--
    2. timeLabel.setText(String.format("%d:%02d", secondsLeft / 60, secondsLeft % 60))
    3. timeLabel.getStyleClass().removeAll("timer-warning", "timer-critical")   // CSS accumulation pitfall prevention
       if (secondsLeft &lt;= 10) timeLabel.getStyleClass().add("timer-critical")
       else if (secondsLeft &lt;= 20) timeLabel.getStyleClass().add("timer-warning")
    4. if (secondsLeft &lt;= 0) endEpisode()

    --- private void endEpisode() ---
    (Single entry point for timer expiry and target-word-guessed; per CLAUDE.md and PROG-01.)
    1. playing = false
    2. gameTimeline.stop()
    3. twistBtn.setDisable(true)
    4. enterBtn.setDisable(true)
    5. lastWordBtn.setDisable(true)
    6. clearBtn.setDisable(true)
    7. letterButtonArea.getChildren().forEach(n -&gt; ((Button) n).setDisable(true))
    (Phase 4 adds: score threshold evaluation, overlay display, level advance or game-over.)

    Button handler wiring (setOnAction calls) must be placed inside startGame() after buildLetterButtons(), so they are set once per game lifecycle. The twistBtn, enterBtn, lastWordBtn, and clearBtn handlers can also be wired in initialize() — either placement is fine as long as they are wired before the game begins.

    Important: Use getStyleClass().remove(String) for individual class removal on guessDisplay to avoid accidentally removing the permanent "guess-display" class. Use getStyleClass().removeAll("timer-warning","timer-critical") on timeLabel because those are the only dynamic classes and .info-label / .time-label are NOT in the removal set.
  </action>
  <verify>
    <automated>cd /Users/andrewmcinnis/Desktop/TextTwist && mvn compile -q 2>&amp;1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - mvn compile -q exits 0
    - PrimaryController.java contains "private void endEpisode()" method
    - PrimaryController.java contains "private void onTick()" method
    - PrimaryController.java contains "gameTimeline.stop()" inside endEpisode
    - PrimaryController.java contains "playing = false" inside endEpisode
    - PrimaryController.java contains "removeAll(\"timer-warning\", \"timer-critical\")" inside onTick
    - PrimaryController.java contains "String.format(\"%d:%02d\"" inside onTick
    - PrimaryController.java contains "Duration.millis(500)" for PauseTransition (both valid and invalid paths)
    - PrimaryController.java contains "guessDisplay.getStyleClass().add(\"guess-valid\")"
    - PrimaryController.java contains "guessDisplay.getStyleClass().add(\"guess-invalid\")"
    - PrimaryController.java contains "guessDisplay.getStyleClass().remove(\"guess-valid\")"
    - PrimaryController.java contains "guessDisplay.getStyleClass().remove(\"guess-invalid\")"
    - PrimaryController.java contains "endEpisode()" called from both onTick (timer expiry path) and enterBtn handler (target word path)
    - PrimaryController.java does NOT contain any setStyle() call
    - PrimaryController.java does NOT contain "java.util.Timer" (must use Timeline)
    - mvn javafx:run launches the game window (human verification step below)
  </acceptance_criteria>
  <done>All handlers implemented; endEpisode() is the single exit point for both timer expiry and target-word-guessed paths; mvn compile exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full PrimaryController implementation: letter button creation, all five button handlers (letter click, Clear, Twist, Enter, Last Word), 120-second Timeline timer, onTick with CSS class management, and endEpisode. CSS feedback classes .guess-valid and .guess-invalid are live in styles.css.
  </what-built>
  <how-to-verify>
    Run: mvn javafx:run (from project root /Users/andrewmcinnis/Desktop/TextTwist)

    Verify each behavior in order:

    1. STARTUP: Window opens immediately with 3 letter buttons visible in the letter area, timer shows "2:00", score shows "Score: 0", level shows "Level: 1". Timer counts down (watch for 1:59, 1:58...).

    2. LETTER CLICK: Click one letter button — it grays out (disabled), a gold tile appears in the guess display area. Click a second — second button grays, second tile appears. Click a third.

    3. CLEAR: Click Clear — all 3 letter buttons re-enable (gold again), guess display empties.

    4. TWIST: Click several letters to build a guess, then click Twist — buttons are replaced with the same letters in a new order, guess display clears. Click Twist again — different order each time (may repeat by random chance).

    5. ENTER — INVALID: Click 1–2 letters, click Enter — the guess display background flashes red for ~500ms, then clears. Letter buttons re-enable.

    6. ENTER — VALID: This requires knowing a valid 3-letter word in the twister_words/3.txt dictionary. Click all 3 letters (forming any valid word the target word contains), click Enter — guess display flashes green for ~500ms, word appears in the found-words list on the left, score updates (e.g., "Score: 90" for a 3-letter word: 3*3*10=90). Last Word button becomes enabled.

    7. LAST WORD: After step 6, click Last Word — the same letters from the last valid word reappear in the guess display; those letter buttons are disabled again.

    8. TIMER STATES: Wait until timer reaches 0:20 — timer label turns amber. Wait until 0:10 — turns red. Let timer reach 0:00 — all buttons disable, timer stops.

    9. TARGET WORD: Restart (close and rerun). Click all letters in the correct order to spell the target word, click Enter — episode ends immediately (no green flash delay), all buttons disable.

    If any step fails, describe the observed vs expected behavior.
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 checks pass, or describe which checks failed and what was observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → PrimaryController | Button clicks are trusted FX events; no raw text entry in Phase 3 |
| PrimaryController → TwistController | In-process method calls; TwistController validates words internally |
| TwistController → WordDictionary → File | File I/O reads from twister_words/ at project root; path is hardcoded, not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-PC-01 | Denial of Service | Timeline / PauseTransition | accept | Single-player local app; no external callers; resource exhaustion not a realistic threat |
| T-03-PC-02 | Tampering | WordDictionary file read | accept | Dictionary files are project assets; no user can substitute the file at runtime in this single-player local app |
| T-03-PC-03 | Information Disclosure | TwistController.getTargetWord() | accept | Target word visible in debug output is intentional for a local class project; no sensitive data |
| T-03-PC-04 | Elevation of Privilege | PauseTransition onFinished | accept | Fires on FX thread; no privilege boundary crossed; handler only clears UI state |
</threat_model>

<verification>
Full phase verification after both plans complete:

1. mvn compile -q exits 0
2. mvn test exits 0 (existing WordDictionary and TwistController JUnit tests still pass)
3. grep "guess-valid" src/main/resources/cs120/lab06/styles.css returns at least one hit
4. grep "guess-correct" src/main/resources/cs120/lab06/styles.css returns no hits
5. grep -c "setStyle" src/main/java/cs120/lab06/PrimaryController.java returns 0
6. grep "java.util.Timer" src/main/java/cs120/lab06/PrimaryController.java returns no hits (Timeline is used instead)
7. grep -c "endEpisode" src/main/java/cs120/lab06/PrimaryController.java returns >= 3 (declaration + 2 call sites: timer expiry and target word match)
8. Human verification checklist in Task 3 passes all 9 steps
</verification>

<success_criteria>
A player opens the app and can:
- Click letter buttons to build a guess (buttons disable, gold tiles appear)
- Clear the guess with Clear (all buttons re-enable, display empties)
- Reshuffle with Twist (new button order, guess clears, Last Word disabled)
- Submit a valid word with Enter (green flash 500ms, score updates, word appears in found list)
- Submit an invalid word with Enter (red flash 500ms, guess clears)
- Recall the last valid word with Last Word (guess rebuilt, buttons disabled)
- Watch the timer count from 2:00 to 0:00 with amber warning at 20s and red at 10s
- Have the episode end automatically at 0:00 with all controls disabled
- Have the episode end immediately upon guessing the target word
All scoring uses n*n*10 where n = length of the guessed word. Both score (cumulative) and levelScore (per-episode) are updated in TwistController on each valid guess.
</success_criteria>

<output>
After completion, create .planning/phases/03-core-game-loop/03-02-SUMMARY.md
</output>
